# Runtime dependencies only. Development and documentation tooling live in
# requirements-dev.txt and requirements-docs.txt so production images do not
# carry pytest/MkDocs and their transitive dependency trees.
fastapi==0.119.0
uvicorn[standard]==0.32.1
httpx==0.28.1
pydantic==2.10.4
playwright==1.61.0
openai==1.82.0
Pillow==11.1.0
redis==8.1.0

# Optional hardened runtime (BROWSER_RUNTIME=camoufox):
#   pip install camoufox==0.4.11 && python -m camoufox fetch
# camoufox==0.4.11
